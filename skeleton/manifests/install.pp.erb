# == Class <%= metadata.name %>::install
#
# This class is called from <%= metadata.name %> for install.
#
class <%= metadata.name %>::install {

  if $::<%= metadata.name %>::manage_package {
    package { $::<%= metadata.name %>::package_name:
      ensure => $::<%= metadata.name %>::package_version,
    }
  }
}
